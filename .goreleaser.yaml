before:
  hooks:
  - go mod tidy
  - go generate ./...
builds:
- env:
  - CGO_ENABLED=0
  goos:
  - linux
  - windows
  - darwin
  main: main.go
  goarch:
  - amd64
  binary: gracefulturnip
  id: gracefulturnip
  ldflags:
  - -s -w
    -X "github.com/taylormonacelli/gracefulturnip/version.Version={{ .Version }}"
    -X "github.com/taylormonacelli/gracefulturnip/version.Date={{ .CommitDate }}"
    -X "github.com/taylormonacelli/gracefulturnip/version.GoVersion={{ .Env.GOVERSION }}"
    -X "github.com/taylormonacelli/gracefulturnip/version.ShortGitSHA={{ .ShortCommit }}"
    -X "github.com/taylormonacelli/gracefulturnip/version.FullGitSHA={{ .FullCommit }}"
archives:
- format: tar.gz
  name_template: |-
    gracefulturnip_ {{- title .Os }}_ {{- if eq .Arch "amd64" }}x86_64
                               {{- else if eq .Arch "386" }}i386
                               {{- else }}
                               {{ .Arch }}
                               {{ end }}
                               {{- if .Arm }}v{{ .Arm }}{{ end }}
  format_overrides:
  - goos: windows
    format: zip
checksum:
  name_template: checksums.txt
snapshot:
  name_template: '{{ incpatch .Version }}-next'
changelog:
  sort: asc
  filters:
    exclude:
    - '^docs:'
    - '^test:'
brews:
- name: gracefulturnip
  directory: Formula
  homepage: https://github.com/taylormonacelli/gracefulturnip
  repository:
    owner: taylormonacelli
    name: homebrew-tools
    branch: main
scoops:
- name: gracefulturnip
  homepage: https://github.com/taylormonacelli/gracefulturnip
  description: "Description of the gracefulturnip project"
  license: MIT
  repository:
    owner: taylormonacelli
    name: scoop-bucket
    branch: master
